version: '3.3'
services:
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: pgadmin
    volumes:
      - "./storage/postgres:/var/lib/postgresql"
    ports:
      - 5432:5432
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

